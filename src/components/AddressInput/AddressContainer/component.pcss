.root
{
  border: 3px solid var(--backgroundColor);
  background-color: white;
  box-sizing: border-box;
  width: 100%;
  position: absolute;
  left: 0;
  right: 0;
  z-index: 3;
}

.hidden
{
  display: none;
}